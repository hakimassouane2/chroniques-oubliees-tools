import { Typography } from "@mui/material";

// Create a base MUI nextJS component with a Typohraphy h1 "HELLO WORLD"
const Index = () => (
  <div>
    <Typography variant="h1">Hello Races</Typography>
  </div>
);

export default Index;
